#include "minishell.h"

int     lex_tokens(ENV)
{
    t_command *cmd;
    t_token *token;

    // while (iter)
    // {
    //     token = iter->data;
    //     //if tok == SEP -> new_cmd
    //     //if tok ==  -> new_cmd

    //     if (0)
    //     {

    //     }
    //     //BUILTIN / CMD
    //     //ARG
    //     //VAR
    //     //SEP ; | || & &&

    //     iter = iter->next;
    // }
    return 0;
}