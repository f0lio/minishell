# TODO for minishell project

## The parsing part

- [ ] Parse a single line with single && double quotes
- [ ] Parse the line with special characters && operators
- [ ] Parse the line with the combination of both of them
- [ ] Multiple line parsing

## && then Execution part
